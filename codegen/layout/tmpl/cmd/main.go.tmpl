package main

import (
	"github.com/spf13/cobra"

	"github/linhbkhn95/kitchen/pkg/logger"

	"github/linhbkhn95/{{ .ProjectName }}/server"
)

func main() {
	cmd := &cobra.Command{
		Use: "rpc-application",
	}

	cmd.AddCommand(&cobra.Command{
		Use: "server",
		Run: func(cmd *cobra.Command, args []string) {
			server.Serve()
		},
	})
	if err := cmd.Execute(); err != nil {
		logger.Fatalf("Error when executed %v", err)
	}
}
